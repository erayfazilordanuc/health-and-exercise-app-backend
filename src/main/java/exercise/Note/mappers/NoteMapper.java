package exercise.Note.mappers;

import org.springframework.stereotype.Component;

@Component
public class NoteMapper {

    // public Note DTOToEntity(NoteDTO noteDTO, Note note) {
    //     Note noteEntity = new Note(noteDTO.)

    //     return noteEntity;
    // }
}
