package exercise.Message.mappers;

import org.springframework.stereotype.Component;

import exercise.Message.dtos.MessageDTO;
import exercise.Message.entities.Message;
import exercise.Message.mappers.MessageMapper;

@Component
public class MessageMapper {

  // public Message DTOToEntity(MessageDTO groupDTO) {
  // Message group = new Message(null, groupDTO.getName(),
  // groupDTO.getAdminId());

  // return group;
  // }
}
