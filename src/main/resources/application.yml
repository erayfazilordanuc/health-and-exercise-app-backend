spring:
  config:
    import: >
      optional:file:./stage.yml,
      optional:file:/etc/hopemove/stage.yml

  application:
    name: Exercise

  datasource:
    url: ${spring.datasource.url}
    username: ${spring.datasource.username}
    password: ${spring.datasource.password}

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  jackson:
    time-zone: Europe/Istanbul

springdoc:
  swagger-ui:
    enabled: true
    operations-sorter: method

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${spring.mail.username}
    password: ${spring.mail.username}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    jwt: DEBUG
    org:
      hibernate:
        sql: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
      springframework:
        web: DEBUG
        security: TRACE
        transaction: DEBUG

jwt:
  secret:
    key: ${jwt.secret.key}

auth:
  admin:
    usernames: ${auth.admin.usernames}
    emails: ${auth.admin.emails}

aws:
  accessKeyId: ${aws.accessKeyId}
  secretKey: ${aws.secretKey}
  region: ${aws.region}
  s3:
    bucket: ${aws.s3.bucket}

swagger:
  server:
    local:
      url: http://localhost:8080
    prod:
